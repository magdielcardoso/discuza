### Task atual
Implementar o modelo de Categorias e exibi-las na página inicial. Popular banco com dados iniciais. Substituir dados mockados por dados reais.

### O que foi feito
- Gerado o model `Category` com os campos: `name:string`, `icon:string`, `color_class:string`.
- Executada a migration para criar a tabela `categories` no banco de dados.
- Atualizado o `HomeController#index` para carregar todas as categorias (`@categories = Category.all`).
- Modificada a view `app/views/home/index.html.erb` para iterar sobre `@categories` e exibir os dados do banco, substituindo os dados mockados anteriores para categorias.
- Atualizado o arquivo `db/seeds.rb` com dados para `Categories`, `Users` e `Discussions`.
- Atualizado o `HomeController#index` para carregar `@recommended_discussions` (as 4 mais recentes).
- Modificada a seção "Recomendadas" na `app/views/home/index.html.erb` para usar `@recommended_discussions` do banco de dados.

### O que falta
- Executar `bin/rails db:seed` para popular o banco de dados com os dados criados.
- Implementar a funcionalidade de filtro por categoria.
- Substituir os dados mockados restantes na página inicial (Tags Populares, Mais Votadas) por dados reais do banco. 